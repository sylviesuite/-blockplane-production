[build]
  command   = "npm run build"
  publish   = "client/dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — all routes serve index.html, client-side router takes over
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
