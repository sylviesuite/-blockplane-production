<Window x:Class="BlockPlane.RevitPlugin.UI.Views.MaterialBrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="BlockPlane Material Browser"
        Height="700" Width="1000"
        MinHeight="500" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    
    <Window.Resources>
        <!-- Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1D4ED8"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#9CA3AF"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#6B7280"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4B5563"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="SearchTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <!-- DataGrid Style -->
        <Style TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F9FAFB"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#EFF6FF"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#DBEAFE"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" Padding="24,20" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="BlockPlane Material Browser" FontSize="24" FontWeight="Bold" Foreground="#111827"/>
                    <TextBlock Text="Search and compare sustainable building materials" FontSize="14" Foreground="#6B7280" Margin="0,4,0,0"/>
                </StackPanel>

                <Button Grid.Column="1" Content="Refresh" Style="{StaticResource SecondaryButton}" 
                        Click="RefreshButton_Click" Padding="20,10"/>
            </Grid>
        </Border>

        <!-- Search and Filters -->
        <Border Grid.Row="1" Background="White" Padding="24,16" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Search Box -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="SearchTextBox" Grid.Column="0" Style="{StaticResource SearchTextBox}"
                             Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                             KeyDown="SearchTextBox_KeyDown">
                        <TextBox.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>

                    <Button Grid.Column="1" Content="Search" Style="{StaticResource PrimaryButton}" 
                            Click="SearchButton_Click" Margin="12,0,0,0" Padding="32,10"/>
                </Grid>

                <!-- Filters -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,0">
                    <TextBlock Text="Category:" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="#374151"/>
                    <ComboBox x:Name="CategoryComboBox" Width="150" Height="36" 
                              SelectedItem="{Binding SelectedCategory}"
                              SelectionChanged="CategoryComboBox_SelectionChanged"/>

                    <TextBlock Text="Min RIS:" VerticalAlignment="Center" Margin="24,0,8,0" Foreground="#374151"/>
                    <TextBox x:Name="MinRISTextBox" Width="60" Height="36" Text="{Binding MinRIS}" 
                             Style="{StaticResource SearchTextBox}" Padding="8"/>

                    <TextBlock Text="Max Carbon:" VerticalAlignment="Center" Margin="24,0,8,0" Foreground="#374151"/>
                    <TextBox x:Name="MaxCarbonTextBox" Width="80" Height="36" Text="{Binding MaxCarbon}" 
                             Style="{StaticResource SearchTextBox}" Padding="8"/>

                    <CheckBox x:Name="RegenerativeOnlyCheckBox" Content="Regenerative Only" 
                              IsChecked="{Binding RegenerativeOnly}" Margin="24,0,0,0" VerticalAlignment="Center"/>

                    <Button Content="Clear Filters" Style="{StaticResource SecondaryButton}" 
                            Click="ClearFiltersButton_Click" Margin="24,0,0,0" Padding="16,8"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Materials Grid -->
        <DataGrid x:Name="MaterialsDataGrid" Grid.Row="2" Margin="24,16"
                  ItemsSource="{Binding Materials}"
                  SelectedItem="{Binding SelectedMaterial}"
                  SelectionChanged="MaterialsDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*" FontWeight="Medium"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*"/>
                <DataGridTextColumn Header="RIS Score" Binding="{Binding RisScore}" Width="80"/>
                <DataGridTextColumn Header="LIS Score" Binding="{Binding LisScore}" Width="80"/>
                <DataGridTextColumn Header="Total Carbon" Width="120">
                    <DataGridTextColumn.Binding>
                        <Binding Path="TotalCarbon" StringFormat="{}{0:F1} kg CO₂"/>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Cost" Width="100">
                    <DataGridTextColumn.Binding>
                        <Binding Path="CostPerUnit" StringFormat="{}${0:F2}"/>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Unit" Binding="{Binding FunctionalUnit}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Action Buttons -->
        <Border Grid.Row="3" Background="White" Padding="24,16" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock x:Name="StatusTextBlock" Text="Ready" VerticalAlignment="Center" Foreground="#6B7280"/>
                    <TextBlock x:Name="MaterialCountTextBlock" Text="" VerticalAlignment="Center" Margin="16,0,0,0" Foreground="#374151" FontWeight="Medium"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="View Details" Style="{StaticResource SecondaryButton}" 
                            Click="ViewDetailsButton_Click" Padding="20,10"
                            IsEnabled="{Binding HasSelectedMaterial}"/>
                    <Button Content="Find Alternatives" Style="{StaticResource PrimaryButton}" 
                            Click="FindAlternativesButton_Click" Margin="12,0,0,0" Padding="20,10"
                            IsEnabled="{Binding HasSelectedMaterial}"/>
                    <Button Content="Close" Style="{StaticResource SecondaryButton}" 
                            Click="CloseButton_Click" Margin="12,0,0,0" Padding="20,10"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Border x:Name="LoadingOverlay" Grid.RowSpan="4" Background="#80000000" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Background="White" Padding="40,30">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Margin="0,0,0,16"/>
                <TextBlock x:Name="LoadingText" Text="Loading materials..." FontSize="14" Foreground="#374151" TextAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
