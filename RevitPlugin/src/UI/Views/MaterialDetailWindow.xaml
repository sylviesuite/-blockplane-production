<Window x:Class="BlockPlane.RevitPlugin.UI.Views.MaterialDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Material Details"
        Height="650" Width="800"
        MinHeight="500" MinWidth="700"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5">
    
    <Window.Resources>
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="ValueText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <Style x:Key="CloseButton" TargetType="Button">
            <Setter Property="Background" Value="#6B7280"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="24,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4B5563"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" Padding="24,20" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
            <StackPanel>
                <TextBlock x:Name="MaterialNameText" FontSize="24" FontWeight="Bold" Foreground="#111827"/>
                <TextBlock x:Name="MaterialCategoryText" FontSize="14" Foreground="#6B7280" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24,20">
                <!-- Basic Information -->
                <TextBlock Text="Basic Information" Style="{StaticResource SectionHeader}"/>
                <Border Background="White" Padding="20" CornerRadius="8" Margin="0,0,0,24">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Manufacturer" Style="{StaticResource LabelText}"/>
                            <TextBlock x:Name="ManufacturerText" Style="{StaticResource ValueText}"/>

                            <TextBlock Text="Functional Unit" Style="{StaticResource LabelText}"/>
                            <TextBlock x:Name="FunctionalUnitText" Style="{StaticResource ValueText}"/>

                            <TextBlock Text="Cost per Unit" Style="{StaticResource LabelText}"/>
                            <TextBlock x:Name="CostText" Style="{StaticResource ValueText}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="RIS Score" Style="{StaticResource LabelText}"/>
                            <TextBlock x:Name="RisScoreText" Style="{StaticResource ValueText}"/>

                            <TextBlock Text="LIS Score" Style="{StaticResource LabelText}"/>
                            <TextBlock x:Name="LisScoreText" Style="{StaticResource ValueText}"/>

                            <TextBlock Text="Regenerative" Style="{StaticResource LabelText}"/>
                            <TextBlock x:Name="RegenerativeText" Style="{StaticResource ValueText}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Carbon Footprint -->
                <TextBlock Text="Carbon Footprint (kg CO₂e)" Style="{StaticResource SectionHeader}"/>
                <Border Background="White" Padding="20" CornerRadius="8" Margin="0,0,0,24">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Production (A1-A3)" Style="{StaticResource LabelText}"/>
                            <TextBlock x:Name="ProductionCarbonText" Style="{StaticResource ValueText}"/>

                            <TextBlock Text="Transport (A4)" Style="{StaticResource LabelText}"/>
                            <TextBlock x:Name="TransportCarbonText" Style="{StaticResource ValueText}"/>

                            <TextBlock Text="Installation (A5)" Style="{StaticResource LabelText}"/>
                            <TextBlock x:Name="InstallationCarbonText" Style="{StaticResource ValueText}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Use Phase (B)" Style="{StaticResource LabelText}"/>
                            <TextBlock x:Name="UsePhaseCarbonText" Style="{StaticResource ValueText}"/>

                            <TextBlock Text="End of Life (C1-C4)" Style="{StaticResource LabelText}"/>
                            <TextBlock x:Name="EndOfLifeCarbonText" Style="{StaticResource ValueText}"/>

                            <TextBlock Text="Total Carbon" Style="{StaticResource LabelText}"/>
                            <TextBlock x:Name="TotalCarbonText" Style="{StaticResource ValueText}" FontWeight="Bold" FontSize="17"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Regional Pricing -->
                <TextBlock Text="Regional Pricing" Style="{StaticResource SectionHeader}"/>
                <Border Background="White" Padding="20" CornerRadius="8" Margin="0,0,0,24">
                    <DataGrid x:Name="RegionalPricingGrid" 
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              GridLinesVisibility="None"
                              HeadersVisibility="Column"
                              Background="Transparent"
                              BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Region" Binding="{Binding Region}" Width="*"/>
                            <DataGridTextColumn Header="Price" Width="120">
                                <DataGridTextColumn.Binding>
                                    <Binding Path="Price" StringFormat="{}${0:F2}"/>
                                </DataGridTextColumn.Binding>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <!-- Certifications -->
                <TextBlock Text="Certifications" Style="{StaticResource SectionHeader}"/>
                <Border Background="White" Padding="20" CornerRadius="8" Margin="0,0,0,24">
                    <TextBlock x:Name="CertificationsText" TextWrapping="Wrap" FontSize="14" Foreground="#374151"/>
                </Border>

                <!-- Description -->
                <TextBlock Text="Description" Style="{StaticResource SectionHeader}"/>
                <Border Background="White" Padding="20" CornerRadius="8" Margin="0,0,0,24">
                    <TextBlock x:Name="DescriptionText" TextWrapping="Wrap" FontSize="14" Foreground="#374151" LineHeight="22"/>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2" Background="White" Padding="24,16" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0">
            <Button Content="Close" Style="{StaticResource CloseButton}" Click="CloseButton_Click" HorizontalAlignment="Right"/>
        </Border>
    </Grid>
</Window>
